{
  "name": "packetcraft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "dev:local": "node scripts/dev-local.mjs",
    "dev:postgres": "set DATABASE_URL=postgres://simtrade:simtrade@127.0.0.1:5432/packetcraft&& tsx watch src/server.ts",
    "generate:assets": "node scripts/generate-assets.mjs",
    "start": "node --enable-source-maps dist/server.js",
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && npm run test",
    "verify": "npm run check",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/static": "^8.0.4",
    "fastify": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.5",
    "three": "^0.179.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^4.0.18"
  }
}
